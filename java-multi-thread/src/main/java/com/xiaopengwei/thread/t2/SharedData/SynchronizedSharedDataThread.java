package com.xiaopengwei.thread.t2.SharedData;/** * <p> * 线程 * 内容：线程间通信 共享数据 * * @author xpwi * @since 2019-04-22 */public class SynchronizedSharedDataThread extends Thread {    private int count = 5;    @Override    public synchronized void run() {        super.run();        //当等于 0 时停止        //此示例不要用 for 语句        //原因：因为使用同步后其他线程就得不到运行的机会了，一直由一个线程进行减法运算        count--;        System.out.println("由" + this.currentThread().getName() + "计算, count=" + count);    }}